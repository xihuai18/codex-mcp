# node-bug fixture (for codex-mcp E2E)

This tiny project is intentionally broken so you can run an end-to-end codex-mcp session that:

1. Runs tests (`npm test`)
2. Fixes the failing test by editing code
3. Re-runs tests and confirms they pass

## Quick check

```bash
npm test
```

Expected: tests fail initially.

